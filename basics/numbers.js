// Challange area

// studentScore
// maxScore
// percent (final output)

// print the percent

let studentScore = 18;
let maxScore = 20;
let percent = (studentScore / maxScore) * 100 + "%";

console.log(percent);
